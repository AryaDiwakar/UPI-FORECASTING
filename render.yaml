services:
  - type: web
    name: upi-forecasting-backend
    rootDir: backend
    runtime: python-3.11
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
